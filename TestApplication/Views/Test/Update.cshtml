@model Model.Models.Test
@{
    var title = "Update";
}

<form method="post">
    <div class="row p-3 border">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="TestId" />
        @if (Model.TestId != 0)
        {
            title = "Edit Test";
        }
        <div class="col-12 border-bottom">
            <h2 class="text-primary">@title</h2>
        </div>

        <div class="col-8 pt-4">
            <div class="form-group row">

                <div class="col-4 ">
                    <label asp-for="Type">Test Type</label>
                </div>
                <div class="col-8 " style="padding-top:0px; margin-bottom:5px">
                    <input asp-for="Type" class="form-control " />
                    <span asp-validation-for="Type" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group row">
                <div class="col-4 offset-4">
                    @if (Model.TestId != 0)
                    {
                        <button type="submit" class="btn btn-primary form-control"> Update test type </button>
                    }
                    else
                    {
                        <button type="submit" class="btn btn-primary form-control"> Create new test </button>
                    }
                </div>
                <div class="col-4">
                    <a asp-action="Index" class="btn btn-success form-control">Back to list</a>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts{
    <patial name="_ValidationScriptsPartial" />
}

